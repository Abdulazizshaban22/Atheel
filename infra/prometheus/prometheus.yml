global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']

  # Example: scrape API metrics when running in the same docker network
  # - job_name: 'atheel-api'
  #   metrics_path: /api/metrics
  #   static_configs:
  #     - targets: ['api:4000']
